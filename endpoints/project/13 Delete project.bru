meta {
  name: 13 Delete project
  type: http
  seq: 13
}

delete {
  url: http://localhost:8080/api/v1/projects/{{projectId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("deletes the project (cascades model + diagrams)", function() {
    expect(res.getStatus()).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
