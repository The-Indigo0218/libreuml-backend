meta {
  name: Admin Login
  type: http
  seq: 11
}

post {
  url: http://localhost:8080/api/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{adminEmail}}",
     "password": "{{adminPas}}"
  }
}

vars:pre-request {
  adminEmail: migopa0222@gmail.com
  adminPas: root
}

settings {
  encodeUrl: true
  timeout: 0
}
